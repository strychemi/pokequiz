<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "PokéQuiz", root_path, class:"navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <%= link_to "Quiz Me!", new_result_path, class: "btn btn-success navbar-btn navbar-left" if signed_in_user? %>

      <%= render "layouts/shared/search_form" if signed_in_user? %>

      <!-- from the application helper -->
      <%= sign_in_out %>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
